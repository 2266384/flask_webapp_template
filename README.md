

models/
Contains the SQLAlchemy models which should be created as individual files for each model

repositories/
Contains database specific operation

routes/
Defines the HTTP routes relating to the app

schemas/
Provides a separate layer for API calls to use

services/
Holds the business logic for the app

static/
Contains folders to hold css, images, and javascript

templates/
HTML template pages

utils/